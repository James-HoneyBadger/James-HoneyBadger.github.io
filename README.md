# James HoneyBadger · GitHub Pages

Two views:

- `index.html` — highlights repositories owned by `@James-HoneyBadger`, ordered by stars (desc). Uses `hb.jpg` on the homepage.
- `repos.html` — searchable, sortable list of repositories owned by `@James-HoneyBadger` (forks excluded), powered by the GitHub REST API.

Site: https://james-honeybadger.github.io
