name: Update pinned repos

on:
  schedule:
    - cron: '23 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-pinned
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate pinned JSON
        env:
          # IMPORTANT:
          # The default Actions token often cannot read profile pinned items via GraphQL.
          # Add a repo secret named PINNED_REPOS_TOKEN (classic PAT with read:user, or fine-grained PAT that can access your profile).
          GITHUB_TOKEN: ${{ secrets.PINNED_REPOS_TOKEN }}
          GH_PINNED_USER: James-HoneyBadger
          GH_PINNED_OUT: data/pinned.json
        run: |
          if [ -z "${GITHUB_TOKEN}" ]; then
            echo "Missing required secret: PINNED_REPOS_TOKEN"
            echo "Add it in: Settings → Secrets and variables → Actions → New repository secret"
            exit 1
          fi
          node scripts/update-pinned.mjs

      - name: Commit pinned JSON
        run: |
          git status --porcelain
          if [ -z "$(git status --porcelain)" ]; then
            echo "No changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/pinned.json
          git commit -m "chore: update pinned repos"
          git push
